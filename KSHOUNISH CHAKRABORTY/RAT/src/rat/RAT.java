//we create a separate package RAT.models where we will have all the classes
package rat;
import java.util.ArrayList;
import rat.models.Staff;
import rat.models.MenuItem;
import rat.models.Order;

public class RAT {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        // TODO code application logic here
       /* MenuItem m1=new MenuItem(1,"dosa",60.0);
        MenuItem m2=new MenuItem(2,"pizza",100.0);

        ArrayList<MenuItem> temp=new ArrayList<MenuItem>();
        temp.add(m1);
        temp.add(m2);
        Order o1=new Order(1,"11:00",temp);
        o1.displayDetails();
*/

    
    }
    
}
